program example1
begin
	n ← (3-c)*a + b*c/d^6 > f[(a*c) % (6^k),b+f,k*abs(h,i*func2(i*k, g % 5))]*max(c+1,d-6,m*f[5,6,7]) ∧ g
	var n,c : int
	var f : array[0 .. 10, 0 .. 5] of int
	{n ← n+(c*3)}
	{n ← (3-c)*a + b*c/d^6 > f[a,b]*max(c, d) ∧ g}
	{abs(n*c+1, 4*9, f[c*3, 7])}
	c ← max(9,8)
	read(n)
	do even(n) → n ← n / 2; c ← (abs(-3)+max(4,5)*8)^3 ≤ 9
	 □ ¬even(n) ∧ n > 1 → n ← 3 * n + 1
	od
	print n
end
